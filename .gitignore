# Binaries
/sight
*.exe
*.dll
*.so

# Local state
.claude/
.codegraph/
.gocache/
coverage.out

# Go workspace (local dev only — each developer creates their own)
go.work
go.work.sum

# macOS
.DS_Store
